<hr>

<div class="container">
  <div class="page-controls">
    <% if prev_page_link.present? %>
      <%= link_to '< PREV', prev_page_link, class: 'prev' %>
    <% end %>

    <% if next_page_link.present? %>
      <%= link_to 'NEXT >', next_page_link, class: 'next' %>
    <% end %>
  </div>
</div>
