<main>
  <div class="container">
    <p class="page-title">MYOB <%= @product.capitalize %></p>

    <%= render @product %>
  </div>
</main>

<%= render partial: "footer", locals: { prev_page_link: client_tasks_path, next_page_link: nil } %>
